version: 0.0
os: linux
files:
 - source: .
   destination: /home/ec2-user/app
hooks:
  AfterInstall:
   - location: scripts/install_keys
     timeout: 300
     runas: root
  ApplicationStart:
   - location: scripts/start_service
     timeout: 300
     runas: root
